class Main
{
    function void main()
    {
        var GameController controll;
        let controll = GameController.new();
        
        while(GameController.getShouldRestart())
        {
            do controll.start();
        }
        do controll.dispose();
        return;
    }
}